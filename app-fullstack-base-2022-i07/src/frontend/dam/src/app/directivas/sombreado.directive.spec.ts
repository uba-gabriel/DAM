import { SombreadoDirective } from './sombreado.directive';

describe('SombreadoDirective', () => {
  it('should create an instance', () => {
    const directive = new SombreadoDirective();
    expect(directive).toBeTruthy();
  });
});
